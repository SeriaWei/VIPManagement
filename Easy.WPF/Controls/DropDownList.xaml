<local:ModelItemControlBase x:Class="Easy.WPF.Controls.DropDownList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Easy.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="315">
    <UserControl.Resources>

    </UserControl.Resources>
    <DockPanel>
        <Label Foreground="{Binding Foreground}" DockPanel.Dock="Left" Content="{Binding Label}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0"></Label>
        <ComboBox  DockPanel.Dock="Right" Name="ComboBox_Value" IsEnabled="{Binding IsEnabled}" VerticalAlignment="Center" HorizontalAlignment="Stretch" DisplayMemberPath="Value" SelectedValuePath="Key" />
    </DockPanel>
</local:ModelItemControlBase>
